{
  "name": "encrypt-and-hashing",
  "version": "1.0.0",
  "description": "- `express from \"express\"` - create server package\r - `logger from \"morgan\"` - HTTP request logger middleware for node.js\r - `cors from \"cors\"` - Enable CORS: allows cross-requests from different addresses.\r - `import Joi from \"joi\"` - Checking request body: schema description language and data validator",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
